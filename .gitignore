# dependencies
/node_modules

# build output
/dist
/build

# secrets — never commit these
.env
.env.*
!.env.example

# uploaded files (runtime data, not source)
/uploads/*
!/uploads/.gitkeep

# logs & caches
*.log
npm-debug.log*
/coverage
.DS_Store
